{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "pnpm --filter eel-wasm run build && pnpm --filter playground run build",
    "test": "pnpm -r run test"
  }
}