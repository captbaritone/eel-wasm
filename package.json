{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "cd packages/compiler && yarn build && cd ../playground && yarn build",
    "test": "cd packages/compiler && yarn ci"
  }
}